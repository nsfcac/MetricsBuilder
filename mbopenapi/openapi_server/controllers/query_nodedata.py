def generate_sqls(node_list: list, measurements: list, start: str, end: str, 
                  interval: str, value: str) -> list:
    """
    Generate sqls from accroding to the user-specified parameters
    """
    meas_list = list(measurements.keys())
    return

def query_nodedata(sqls: list, client: object) -> dict:
    """
    Query node data
    """
    return